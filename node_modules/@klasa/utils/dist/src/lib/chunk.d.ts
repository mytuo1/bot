/**
 * Splits up an array into chunks
 * @since 0.5.0
 * @param entries The object to be merged
 * @param chunkSize The object to merge
 */
export declare function chunk<T>(entries: readonly T[], chunkSize: number): T[][];
