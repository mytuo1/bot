"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isThenable = void 0;
const isFunction_1 = require("./isFunction");
function hasThen(input) {
    return Reflect.has(input, 'then') && isFunction_1.isFunction(input.then);
}
function hasCatch(input) {
    return Reflect.has(input, 'catch') && isFunction_1.isFunction(input.catch);
}
/**
 * Verify if an object is a promise.
 * @param input The promise to verify
 */
function isThenable(input) {
    if (typeof input !== 'object' || input === null)
        return false;
    return (input instanceof Promise) ||
        (input !== Promise.prototype && hasThen(input) && hasCatch(input));
}
exports.isThenable = isThenable;
//# sourceMappingURL=isThenable.js.map