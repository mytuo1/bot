"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mergeDefault = void 0;
const deepClone_1 = require("./deepClone");
const isObject_1 = require("./isObject");
/**
 * Sets default properties on an object that aren't already specified.
 * @since 0.5.0
 * @param def Default properties
 * @param given Object to assign defaults to
 */
function mergeDefault(defaults, given) {
    if (!given)
        return deepClone_1.deepClone(defaults);
    for (const [key, value] of Object.entries(defaults)) {
        const givenValue = Reflect.get(given, key);
        if (typeof givenValue === 'undefined') {
            Reflect.set(given, key, deepClone_1.deepClone(value));
        }
        else if (isObject_1.isObject(givenValue)) {
            Reflect.set(given, key, mergeDefault(value, givenValue));
        }
    }
    return given;
}
exports.mergeDefault = mergeDefault;
//# sourceMappingURL=mergeDefault.js.map